package com.shpp.cs.assignments.arrays;

public class Main {

    public static void main(String[] args) {
	    // no no, don't do anything here plz
        // just look at subpackages!!
        int a = 3;
        int b = 2;
        int c = 3;

        System.out.println((a != b) + " " + (a != c));
        System.out.println((a != b) && (a != c));
        System.out.println("Вверху И внизу ИЛИ");



        System.out.println((a != b) +" "+ (a != c));
        System.out.println((a != b) || (a != c));
    }
}
